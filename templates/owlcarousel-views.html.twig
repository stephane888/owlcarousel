<div {{wrappers_attributes}}>
	<div{{attributes}}>
		{% if title %}
			<h3>{{ title }}</h3>
		{% endif %}
		{% for row in rows %}
			<div{{row.attributes}}>{{ row }}</div>
		{% endfor %}
	</div>
</div>